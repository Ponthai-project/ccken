# 実践ハーネスエンジニアリング

- 登壇: 梶川 琢馬（@kajitack）／株式会社TechBowl VPoT（TechTrainの開発・メンター）
- 元ファイル: `実践ハーネスエンジニアリング.pdf`（19p）

## Claude Code導入から1年

- 実装の完結: エディタ不要、GitHub CLIでissueやプルリク作成
- 並列実行: worktree、Subagents、Agent Teams
- 長期実行: Auto mode、`--dangerously-skip-permissions`
- → **長期実行させるための自律性と安全性**に注目。

## Harness Engineeringとは

自律的なAI Agentの実現のために、LLMモデルの性能を最大限引き出すための環境を設計すること（ハーネス＝馬具・手綱）。構成要素は3つ:

1. コンテキスト設計
2. アーキテクチャ制約とガードレール
3. 品質評価ループ

## 1. コンテキスト設計（ドキュメントの集約とエージェントの分離）

- **ドキュメントの集約**: notion・whimsical・linearなど開発ドキュメントをGitHubに集約（MCPやCLIなしで知識に、履歴も知識に）。ドメインモデル管理にYAMLを使用。
- **プロジェクト管理の集約**: activeな158リポジトリを横断してプロジェクト管理・仕様調査。skillを作成（`/investigate`で調査して`/sub-issue`でissueを切る）。
- **エージェントを分離**（実装・品質・性能・仕様・Issue作成を分離し適切なコンテキストを保つ）:

| エージェント | 責務 | 特徴 |
|---|---|---|
| tdd-web-engineer | TDD実装 | Red→Green→Refactor厳守 |
| code-reviewer | コード品質 | test-conventions準拠 |
| perf-reviewer | 性能・セキュリティ | OWASP Top 10, N+1検出 |
| spec-reviewer | 仕様整合性 | 過剰防御の排除 |
| task-issue-creator | Issue分割 | DDD層境界で分割 |

`/dev`スキルがtdd-web-engineerに委譲、`/review`スキルが3エージェント並列レビューを実行。

## 2. アーキテクチャ制約とガードレール（依存方向や型チェック）

- **依存方向の定義と強制**: BEはクリーンアーキテクチャ＋deptrac、FEはmonorepo & container/presentation＋dependency-cruiser。
- **多層の静的品質担保**:

| レイヤー | 手段 | 効果 |
|---|---|---|
| 型システム | ValueObject / 集約の定義 | 不正な値・不変条件違反を防ぐ |
| 静的解析 | deptrac / phpstan / ESLint / Biome | 依存違反・型エラー |
| テスト | ユニット + E2E + VRT | 振る舞いの変更を自動検知 |

## 3. 品質評価ループ（AI自身が自己検証と修正する）

- **多層防御**: コンテキスト整備→型システム→静的解析→テスト→AIレビュー→人間のレビュー。各レイヤーが異なる観点で問題を自動検出し、人間に届く前にフィルタリングする。
- 開発・品質検証用skill: `/dev`（仕様把握→テスト計画→TDD実装→品質チェック）→品質ゲート（Fail 3回でIssue化、Passで`/pr`）→`/pr`（`bin/create-pr`で合法的な経路でPR作成）→`/review`（3エージェント並列レビュー）→`/fix-pr`（指摘の修正→再レビュー）→Merge。
- **エージェントに「目」を与える**: Chrome DevTools MCPで実際にブラウザを操作させ自動検証（パス特定→APIサーバ起動/Staging接続→ページを開く→スクショ＋コンソールエラー確認）。**CIが通っただけで「完了」と宣言させず、実際のブラウザで動作を確認する**。

## まとめ

- やってることは今までと変わらないが、開発＝ハーネスエンジニアリング中心になってきた。
- アーキテクチャやテスト手法、計測など開発改善のアイデアを知っている必要がある。
- 次の関心: 監視ツールとの連携、ドメイン知識の整理、事業の意思決定、採用・育成へのAI活用と人間のバランス。
